---
proposes-merge-into: depths/no-such-page.md
---

# Rumor of the Vault

A proposal naming a merge target that does not exist. It is DM-only
and correctly marked, so the only complaint is the dangling target —
proving the check reads the effective wiki rather than trusting the
frontmatter. Nobody links this page and it sits outside the catalog,
which for a proposal is the expected shape, not a defect.
