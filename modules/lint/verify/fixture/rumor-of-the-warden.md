---
visibility: player
proposes-merge-into: warden.md
curation: reviewed
ingest: sideways
lint: maybe
---

# Rumor of the Warden

New lore from the table, landed as a transient proposal: the players
now claim the [Warden](warden.md) took the oath twice, once at the
water line and once before it. The page dies on merge, so it is held
to none of canon's editorial standards while it waits — but its
markers are nonsense, and every one of them is caught: `curation`,
`ingest`, and `lint` all carry values outside their sets, and an
unadjudicated proposal may not claim player visibility.
