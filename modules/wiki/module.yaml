name: wiki
version: 0.6.0
summary: >
  The campaign knowledge architecture: a DM-master wiki schema with
  fail-closed page-level visibility, twin pages for progressive
  reveal, authorship-marked sources, a typed operation log, and the
  deterministic player projection (the `project` verb) that refuses
  to ship a firewall breach. Carries the pen axis — tri-state
  authorship, curation asserted separately from it, literal vs
  derived ingest — and the propose-adjudicate-merge flow that lands
  new table lore as a transient page the DM adjudicates.
fragility: durable
walk_away: >
  Nothing: the campaign is markdown in a git repo and the projection is
  stdlib Python the campaign owns, so the whole knowledge base outlives
  every service around it.
touches:
  - AGENTS.md
  - CLAUDE.md
  - wiki/
  - sources/
  - .eddic/lib/project.py
depends:
  - cli
  - lint
cost_posture:
  free: Entirely local and free (markdown files + stdlib projection).
  paid: none
  heuristic: n/a
compatibility:
  claude:
    role: maintaining agent
    status: verified
    date: 2026-07-16
    evidence: CLAUDE.md stub-import convention verified against code.claude.com docs July 2026; campaigns maintained by Claude Code
